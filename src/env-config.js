(function (window) {
    window.__env = window.__env || {};
  
    // API url
    window.__env.post='https://recipes-mine.firebaseio.com/'
  
    // Whether or not to enable debug mode
    // Setting this to false will disable console output
    window.__env.enableDebug = true;
    window.__env.showLoader = true;

    window.__env.apiKey="AIzaSyDO9avzbc-NuehPt5EoTr8tQFmyzg7qQF8"
    window.__env.resetPassword="https://identitytoolkit.googleapis.com/v1/accounts:resetPassword"
    window.__env.forgotPassword="https://identitytoolkit.googleapis.com/v1/accounts:sendOobCode"
    window.__env.signIn= "https://identitytoolkit.googleapis.com/v1/accounts:signInWithPassword"
    window.__env.signUp="https://identitytoolkit.googleapis.com/v1/accounts:signUp"

    window._
  }(this));